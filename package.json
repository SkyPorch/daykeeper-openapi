{
  "name": "@skyporch/daykeeper-openapi-workspace",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "bundle": "redocly bundle openapi/daykeeper.yaml --output dist/daykeeper.openapi.yaml && redocly bundle openapi/customer.yaml --output dist/customer.openapi.yaml",
    "check": "pnpm format:check && pnpm lint && pnpm bundle",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "redocly lint openapi/daykeeper.yaml openapi/customer.yaml"
  },
  "devDependencies": {
    "@redocly/cli": "2.48.0",
    "prettier": "3.9.6"
  }
}
